apply plugin: "war"

bootWar {
    mainClassName = "com.ne.microservice.NemicroserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
